#' A demo `eeg_epochs` dataset
#'
#' This is a small dataset with 11 electrodes and 80 epochs used for
#' demonstrating some features of the `eegUtils` package.
#'
#' @format An `eeg_epochs` object
"demo_epochs"

#' A demo `eeg_epochs` dataset
#'
#' This is a small dataset with 23 electrodes and 80 epochs used for
#' demonstrating some features of the `eegUtils` package. It is part of a
#' recording from a visual spatial cueing task.
#'
#' @format An `eeg_epochs` object
"demo_spatial"
