# Calculate circular mean

Calculates the circular mean from vector of phase angles. Input should
be in radians.

## Usage

``` r
circ_mean(data)
```

## Arguments

- data:

  vector of phase angles in radians
