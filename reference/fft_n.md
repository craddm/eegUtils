# N-point FFT

Either zero-pads by adding trailing zeroes, or truncates a signal to N
and runs an FFT.

## Usage

``` r
fft_n(signal, n)
```

## Arguments

- signal:

  signal to be FFT'd

- n:

  Number of FFT points
