# Internal function for processing epoch_events during selection

Converts character strings into a vector of epoch numbers with matching
labels.

## Usage

``` r
proc_events(epoch_events, event_type, epoch_nos, event_labels, keep)
```
