# Rearrange and combine `tfr_average` objects

Epochs in different `tfr_average` objects are not necessarily in the
same order.

## Usage

``` r
rearrange_tfr(data, add_obj)
```

## Arguments

- data:

  first object to be combined

- add_obj:

  list of additional objects
